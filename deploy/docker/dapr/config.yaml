apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: flowforge-config
spec:
  tracing:
    samplingRate: "1"
    otel:
      endpointAddress: "http://jaeger:4317"
      isSecure: false
      protocol: grpc
  features:
    - name: AppHealthCheck
      enabled: true
